{
  "name": "nysub",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nysub",
    "create-local": "graph create --node http://localhost:8020/ nysub",
    "remove-local": "graph remove --node http://localhost:8020/ nysub",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nysub",
    "test": "graph test"
  },
  "dependencies": {
    "@apollo/client": "^3.8.4",
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
